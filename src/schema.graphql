
type Query {
    users(query: String): [User!]!
    # TODO: 1. Add "posts" query that will return non null array of Post type

    # TODO: 3. Add query parameter to "posts" query
    me: User!
    post: Post!
}

type User {
    id: ID!
    name: String!
    email: String!
    age: Int
}

type Post {
    id: ID!
    title: String!
    body: String!
    published: Boolean!
}
